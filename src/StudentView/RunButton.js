import React from 'react';

function RunButton() {
  return (
    <button id="run-button" onClick={handleClick}>
        Run code
    </button>
  );
}

function handleClick(e) {

}
export default RunButton;
